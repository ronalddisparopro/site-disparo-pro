---
import BaseLayout from "./BaseLayout.astro";
import type { SeoProps } from "../types/seo";
import Section from "../components/ui/Section.astro";
import Heading from "../components/ui/Heading.astro";

export interface Props {
  seo: SeoProps;
  title: string;
  description?: string;
}

const { seo, title, description } = Astro.props;
---

<BaseLayout seo={seo}>
  <Section class="bg-surface-muted border-b border-border-default">
    <div class="max-w-3xl mx-auto text-center">
      <Heading as="h1" class="mb-4">{title}</Heading>
      {description && <p class="text-xl text-text-secondary">{description}</p>}
    </div>
  </Section>
  <Section>
    <div class="flex flex-col lg:flex-row gap-12">
      <div class="grow">
        <slot />
      </div>
      <aside class="w-full lg:w-80 shrink-0">
        <div
          class="bg-surface-default p-6 rounded-lg shadow-sm border border-border-default"
        >
          <Heading as="h3" class="mb-4">Categorias</Heading>
          <ul class="space-y-2">
            <li>
              <a
                href="/blog/categoria/whatsapp"
                class="text-primary hover:underline">WhatsApp</a
              >
            </li>
            <li>
              <a href="/blog/categoria/rcs" class="text-primary hover:underline"
                >RCS</a
              >
            </li>
          </ul>
        </div>
      </aside>
    </div>
  </Section>
</BaseLayout>
