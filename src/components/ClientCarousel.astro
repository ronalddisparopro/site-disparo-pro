---
import { Image } from "astro:assets";
import acordoCerto from "../assets/images/clients/acordo-certo.svg";
import agibank from "../assets/images/clients/agibank.svg";
import chapecoense from "../assets/images/clients/chapecoense.svg";
import chinaBox from "../assets/images/clients/china-box.svg";
import gendai from "../assets/images/clients/gendai.svg";
import ixcsoft from "../assets/images/clients/ixcsoft.svg";
import modal from "../assets/images/clients/modal.svg";
import spoleto from "../assets/images/clients/spoleto.svg";

const clients = [
  { name: "Acordo Certo", src: acordoCerto },
  { name: "Agibank", src: agibank },
  { name: "Chapecoense", src: chapecoense },
  { name: "China in Box", src: chinaBox },
  { name: "Gendai", src: gendai },
  { name: "IXCSoft", src: ixcsoft },
  { name: "Modal", src: modal },
  { name: "Spoleto", src: spoleto },
];
---

<section class="mx-auto py-12 bg-surface-default overflow-hidden max-w-[985px]">
  <div
    class="relative w-full flex flex-nowrap overflow-hidden mask-[linear-gradient(to_right,transparent_0,black_128px,black_calc(100%-128px),transparent_100%)]"
  >
    <div
      class="flex items-center justify-center md:justify-start [&_li]:mx-6 [&_img]:max-w-none animate-infinite-scroll py-2"
    >
      <ul
        class="flex items-center justify-center md:justify-start [&_li]:mx-6 [&_img]:max-w-none"
      >
        {
          clients.map((client) => (
            <li>
              <Image
                src={client.src}
                alt={client.name}
                class="h-5 md:h-7 w-auto transition-all duration-300 pointer-events-none select-none"
                height={28}
              />
            </li>
          ))
        }
      </ul>
      <ul
        class="flex items-center justify-center md:justify-start [&_li]:mx-6 [&_img]:max-w-none"
        aria-hidden="true"
      >
        {
          clients.map((client) => (
            <li>
              <Image
                src={client.src}
                alt={client.name}
                class="h-5 md:h-7 w-auto transition-all duration-300 pointer-events-none select-none"
                height={28}
              />
            </li>
          ))
        }
      </ul>
    </div>
  </div>
</section>

<style>
  .animate-infinite-scroll {
    animation: infinite-scroll 30s linear infinite;
  }

  @keyframes infinite-scroll {
    from {
      transform: translateX(0);
    }
    to {
      transform: translateX(-50%);
    }
  }

  /* Opção de pausar ao hover se desejar, mas para logos corporativos às vezes é melhor manter girando */
  .animate-infinite-scroll:hover {
    animation-play-state: paused;
  }
</style>
